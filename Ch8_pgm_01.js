// Creating arrays
var scores;
var names;
var akaScores;

scores = [ 3, 1, 8, 2, 5 ];  // Adding an extra element to scores
names = [ "Kandra", "Dax", "Blinky", "Jax" ];  // Adding an extra element to names

console.log(scores);
console.log(names);

akaScores = scores;  // Assigning scores array to akaScores

console.log(akaScores);



/* Further Adventures
 *
 * 1) Add an extra element to each array.
 *
 * 2) Declare a third variable, akaScores.
 *
 * 3) Assign the scores array to akaScores.
 *    akaScores = scores
 *
 * 4) Log akaScores to the console.
 *
 */